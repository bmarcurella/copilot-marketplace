---
description: "{{Use when... include trigger phrases so a parent agent knows when to delegate}}"
name: "{{Agent Name}}"
tools: [read, search]
user-invocable: true
---

You are a specialist at {{specific task}}. Your job is to {{clear, single purpose}}.

## Constraints
- DO NOT {{something this agent must never do}}
- ONLY {{the one thing this agent does}}

## Approach
1. {{step one}}
2. {{step two}}
3. {{step three}}

## Output Format
{{exactly what this agent should return}}
